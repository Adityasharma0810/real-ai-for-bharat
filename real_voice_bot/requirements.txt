# Combined dependencies for AI SkillFit — FastAPI backend + Priya voice agent
# Used for Render deployment

# ── Backend (FastAPI server) ──────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sqlalchemy>=2.0.0
python-dotenv==1.2.2
livekit-api>=0.8.0
pydantic>=2.0.0
supabase>=2.0.0
httpx>=0.27.0

# ── Agent (Priya voice bot) ───────────────────────────────────────────────────
livekit-agents[codecs]==1.5.6
livekit-plugins-sarvam==1.5.6
langchain-groq==1.1.2
